# Copy to .env and replace every change-me value before `./stack.sh up`.
ICD_DB_NAME=icd_learning
ICD_DB_USER=icd_app
ICD_DB_PASSWORD=change-me-app-db-password
ICD_DB_ROOT_PASSWORD=change-me-root-db-password
APP_HTTP_PORT=8080
